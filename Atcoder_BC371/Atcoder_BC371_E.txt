Atcoder-ABC371-E

# [ABC371E] I Hate Sigma Problems

## 题面翻译

# [ABC371E] I Hate Sigma Problems

## 题目描述

[problemUrl]: https://atcoder.jp/contests/abc371/tasks/abc371_e

给定一个长度为 $ N $ 的整数序列 $ A=(A_1,A_2,\ldots,A_N) $. 我们还可以通过以下方式定义 $ f(l,r) $。

- $ (A_l,A_{l+1},\ldots,A_{r-1},A_{r}) $ 中值的类型数。
 
求以下表达式的值。

 $ \displaystyle\\sum_{i=1}^{N}\sum_{j=i}^N\ f(i,j) $

## 输入格式

输入以下列形式从标准输入中给出。

> $ N $ $ A_1 $ $ \ldots $ $ A_N $

## 案例输入形式

输出答案。

### 样例 #1

### 样例输入 #1

```
3
1 2 2
```

### 模式传送带 #1

```
8
```

### 示例 #2

### pattern_construct #2

```
9
5 4 2 2 3 2 4 4 1
```

### 模式传送 #2

```
111
```

### 演示

### Constraints.

- $ 1\leq\ N\leq\ 2\times\ 10^5 $
- $ 1\leq\ A_i\leq\ N $
- 所有输入的数字都是整数
 
### 示例解释 1

考虑 $ f(1,2) $. $ (A_1,A_2)=(1,2) $ $ (A_1,A_2)=(1,2) $ 中值的类型数是 $ 2 $, 所以 $ f(1,2)=2 $. 考虑 $ f(2,3) $。 $ (A_2,A_3)=(2,2) $ 包含 $1$，所以 $ f(2,3)=1 $。$ f $ 的和为 $ 8 $。

通过DeepL.com（免费版）翻译

## 题目描述

[problemUrl]: https://atcoder.jp/contests/abc371/tasks/abc371_e

長さ $ N $ の整数列 $ A=(A_1,A_2,\ldots,A_N) $ が与えられます。また、$ f(l,r) $ を以下で定義します。

- $ (A_l,A_{l+1},\ldots,A_{r-1},A_{r}) $ に含まれる値の種類数
 
次の式の値を求めてください。

 $ \displaystyle\ \sum_{i=1}^{N}\sum_{j=i}^N\ f(i,j) $

## 输入格式

入力は以下の形式で標準入力から与えられる。

> $ N $ $ A_1 $ $ \ldots $ $ A_N $

## 输出格式

答えを出力せよ。

## 样例 #1

### 样例输入 #1

```
3
1 2 2
```

### 样例输出 #1

```
8
```

## 样例 #2

### 样例输入 #2

```
9
5 4 2 2 3 2 4 4 1
```

### 样例输出 #2

```
111
```

## 提示

### 制約

- $ 1\leq\ N\leq\ 2\times\ 10^5 $
- $ 1\leq\ A_i\leq\ N $
- 入力される数値は全て整数
 
### Sample Explanation 1

$ f(1,2) $ について考えます。$ (A_1,A_2)=(1,2) $ に含まれる値の種類数は $ 2 $ なので $ f(1,2)=2 $ です。 $ f(2,3) $ について考えます。$ (A_2,A_3)=(2,2) $ に含まれる値の種類数は $ 1 $ なので $ f(2,3)=1 $ です。 $ f $ の総和は $ 8 $ となります。

#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef unsigned long long ull;
typedef vector<ll> vl;
typedef vector<int> vi;
typedef stack<ll> sl;
typedef stack<int> si;
typedef queue<ll> ql;
typedef queue<int> qi;
typedef map<int, int> mii;

struct T {
  ll e, id;
} a[200010];

int n;
ll f;

bool cmp(T x, T y) { return x.e < y.e || (x.e == y.e && x.id < y.id); }

int main() {
  ios::sync_with_stdio(false);
  cin.tie(0);
  cin >> n;
  for (int i = 1; i <= n; i++) {
    cin >> a[i].e;
    a[i].id = i;
  }
  sort(a + 1, a + n + 1, cmp);
  for (int i = 1; i <= n; i++) {
    if (i == 1 || a[i].e != a[i - 1].e)
      f += a[i].id * (n + 1 - a[i].id);
    else
      f += (a[i].id - a[i - 1].id) * (n + 1 - a[i].id);
  }
  cout << f << "\n";
  return 0;
}